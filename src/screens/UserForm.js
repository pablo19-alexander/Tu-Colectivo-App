import React, { useState } from "react";
import { Text, StyleSheet, View } from "react-native";

export default function UserForm() {
  return (
    <View>
      <Text> formulario de usuarios </Text>
    </View>
  );
}

const styles = StyleSheet.create({});
