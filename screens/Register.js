import React, { useState } from "react";
import { Text, StyleSheet, View } from "react-native";

export default function Register() {
  return (
    <View>
      <Text> Aca se realiza el proceso del resgistro de usuario </Text>
    </View>
  );
}

const styles = StyleSheet.create({});
